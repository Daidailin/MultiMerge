# CLI 文案更新列表

> **版本**: v1.0
> **日期**: 2026-03-21
> **依据**: [multimerge-behavior-spec-v1.md](./multimerge-behavior-spec-v1.md)

---

## 一、需要更新的 CLI 文案

### 1.1 帮助文案

**文件**: `cli/main.cpp`

| 行号 | 当前文案 | 应改为 | 原因 |
|------|----------|--------|------|
| 135 | `输入文件列表（至少 1 个）` | `输入文件列表（至少 2 个）` | 与引擎约束一致 |

### 1.2 错误文案

| 行号 | 当前文案 | 应改为 | 原因 |
|------|----------|--------|------|
| 276 | `请至少指定一个输入文件` | `请至少指定两个输入文件` | 与引擎约束一致 |

---

## 二、完整的 CLI 帮助文本（更新后）

```
MultiMerge 数据文件合并工具 v1.0
========================================

用法: MultiMerge [选项] <输入文件1> <输入文件2> [输入文件3] ...

输入文件:
  输入文件列表（至少 2 个）

选项:
  -o, --output <文件名>       输出文件路径
  -i, --interpolation <方法>  插值方法（nearest/linear）
  -d, --delimiter <分隔符>    输出分隔符（space/comma/tab）
  -e, --encoding <编码>       输出编码（UTF-8/GBK）
  -t, --tolerance <微秒>     时间容差（0=精确匹配，-1=无限制）
  -E, --engine <引擎>         合并引擎：legacy（旧引擎）, stream（新流式引擎）
  -v, --verbose               详细输出模式
  -h, --help                  显示帮助
  --version                   显示版本号
```

---

## 三、影响分析

### 3.1 用户可见变化

| 变化 | 影响 |
|------|------|
| 帮助文案 | 用户更清楚需要 2 个文件 |
| 错误提示 | 错误信息更准确 |

### 3.2 向后兼容性

- 原有用法（1个文件）会报错，提示更清晰
- 其他参数不受影响

---

## 四、执行计划

| 步骤 | 操作 | 优先级 |
|------|------|--------|
| 1 | 修改 main.cpp 行135 | P1 |
| 2 | 修改 main.cpp 行276 | P1 |
| 3 | 编译验证 | P1 |
| 4 | 测试验证 | P1 |

---

**状态**: 待执行
